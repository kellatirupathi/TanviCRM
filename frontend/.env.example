# ── TanviCRM Frontend Environment ─────────────────────────────
# Copy to `.env` if you need to override the API base URL.
# In development, Vite proxies /api to the backend (see vite.config.js),
# so this is only needed for production builds pointing at a remote API.

# VITE_API_URL=https://api.your-domain.com/api
